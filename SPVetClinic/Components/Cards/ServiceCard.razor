@* ============================================
   SERVICE CARD - Componente Reutilizable
   Uso: <ServiceCard ImageSrc="images/service.svg" Icon="🩺" Title="Consulta" Description="..." />
   ============================================ *@

@if (string.IsNullOrEmpty(Link))
{
    <div class="service-card reveal stagger-@StaggerIndex">
        <div class="service-card-image-wrapper">
            <img src="@ImageSrc" alt="@ImageAlt" class="service-card-image" />
            <div class="service-card-icon">@Icon</div>
        </div>
        <div class="service-card-content">
            <h3 class="service-card-title">@Title</h3>
            <p class="service-card-description">@Description</p>
            @ChildContent
        </div>
    </div>
}
else
{
    <a href="@Link" class="service-card reveal stagger-@StaggerIndex">
        <div class="service-card-image-wrapper">
            <img src="@ImageSrc" alt="@ImageAlt" class="service-card-image" />
            <div class="service-card-icon">@Icon</div>
        </div>
        <div class="service-card-content">
            <h3 class="service-card-title">@Title</h3>
            <p class="service-card-description">@Description</p>
            @if (!string.IsNullOrEmpty(LinkText))
            {
                <span class="service-card-link">@LinkText</span>
            }
            @ChildContent
        </div>
    </a>
}

@code {
    [Parameter] public string ImageSrc { get; set; } = "";
    [Parameter] public string ImageAlt { get; set; } = "";
    [Parameter] public string Icon { get; set; } = "";
    [Parameter] public string Title { get; set; } = "";
    [Parameter] public string Description { get; set; } = "";
    [Parameter] public string Link { get; set; } = "";
    [Parameter] public string LinkText { get; set; } = "";
    [Parameter] public int StaggerIndex { get; set; } = 1;
    [Parameter] public RenderFragment? ChildContent { get; set; }
}